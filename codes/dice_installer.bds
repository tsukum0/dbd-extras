$var[z;\]]
$var[y;\\\\;]
$onlyIf[$varExists[d]==true;Create `d` variable in the app!]

$setVar[d;
$$c[]httpGet[https://raw.githubusercontent.com/tsukum0/dbd-extras/refs/heads/main/plugins/d.bds$var[z]
$$c[]setVar[installer$var[y]$$c[]httpResult$var[z]
]

$setVar[installer;]
use `$if[$varExists[prefix]]$getVar[prefix]$endifd`!
