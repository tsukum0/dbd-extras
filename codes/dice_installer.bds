$c[+ installer here]
$onlyIf[$varExists[d];Create `d` variable in the app!]

$setVar[d;
$httpGet[<dice_repo>]
$setVar[installer;$httpResult]
]

$setVar[installer;]
1/3
use `$if[$varExists[prefix]]$getVar[prefix]$endifd`!
